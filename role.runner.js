const { offload } = require('role.actions');

var roleRunner = {
    /** @param {Creep} creep **/
    run: function (creep) {
        // look for dropped items
        // bring special resources from containers to terminal/lab
        // fill extensions from stoarage
    }
}

module.exports = roleRunner;